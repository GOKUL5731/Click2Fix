name: click2fix_worker_app
description: Click2Fix worker app.
publish_to: "none"
version: 0.1.1+2

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2
  dio: ^5.7.0
  firebase_core: ^3.15.2
  firebase_messaging: ^15.2.9
  socket_io_client: ^3.0.2
  image_picker: ^1.1.2
  geolocator: ^13.0.2
  google_maps_flutter: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/

